# MQ Provisioning Service - Environment Configuration
# Copy this file to .env and fill in your values

# ===================
# Backend Configuration
# ===================

# Port for backend API (default: 8080)
BACKEND_PORT=8080

# Git credentials for cloning repositories
GIT_USERNAME=your-git-username
BITBUCKET_TOKEN=your-bitbucket-access-token

# Spring profile (default, dev, prod)
SPRING_PROFILES_ACTIVE=default

# Logging level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# ===================
# Frontend Configuration
# ===================

# Port for frontend (default: 3000)
FRONTEND_PORT=3000
